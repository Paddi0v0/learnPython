a = [11,22,33]
b = [44,55]

如果想把b列表里的所有元素添加到a列表里去:
a.extend(b)

如果使用a.append(b),则会出现如下情况:
[11,22,33[44,55]]

如果使用a.extend('test'),则返还值是,即a是,可以写Str(字符串),但是不可以写整型(int):
[11,22,33,'t','e','x','t']

append相当于整体添加，而extend则是拆分之后(遍历)添加,有点for..in...的意思
